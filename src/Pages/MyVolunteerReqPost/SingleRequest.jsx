import React from 'react';

const SingleRequest = ({ data }) => {
    console.log(data);
    return (
        <div>
            <h1>This is single need section</h1>

        </div>
    );
};

export default SingleRequest;